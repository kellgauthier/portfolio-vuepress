---
label: About
hide_body: true
fields:
- name: header
  type: text
  config:
    required: false
  label: Header
- name: blocks
  type: blocks
  template_types: []
  config:
    min: 
    max: 
  label: blocks
